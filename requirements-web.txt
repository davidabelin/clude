# What the Cloud Run image installs (the Dockerfile), and nothing more.
# The web app needs Flask (with Werkzeug and Jinja), gunicorn to serve it,
# google-cloud-storage for its gs:// store and, since Phase 8.3a, the
# anthropic SDK for the model seats at a table, which the service reaches
# with the key Secret Manager hands it (docs/web.md, "Deploying").
# requirements.txt is the developer's list and adds the test runner and
# Playwright.
flask>=3.1
gunicorn>=23.0
google-cloud-storage>=3.0
anthropic>=1.5
