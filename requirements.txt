pytest>=8.0
# Runs the suite in parallel: `python -m pytest -q -n auto` (about half the
# serial ~220 s on Orbit). Optional; plain `pytest` still works.
pytest-xdist>=3.8
# Only needed for gs:// record stores (clude_storage.GcsStore); everything
# else is the standard library, and the package is imported lazily.
google-cloud-storage>=3.0
# Only needed to pilot characters with a real model (clude_llm's
# AnthropicBackend); imported lazily, so the suite and every other backend
# run without it. Credentials: ANTHROPIC_API_KEY (docs/architecture.md).
anthropic>=1.5
# The web app (clude_web, Phase 8.1). Werkzeug comes with Flask and is
# what hashes account passwords; Jinja is its templating. Nothing outside
# clude_web and `clude_cli.py users` imports any of them, so the engine
# and the agents still run on the standard library alone.
flask>=3.0
